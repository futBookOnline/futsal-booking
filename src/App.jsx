import { Outlet } from "react-router-dom";
const App = () => {
  return (
    <>
      {/*The outlet form the router is displayed here*/}
      <Outlet />
      {/*The outlet form the router is displayed here*/}
    </>
  );
};

export default App;
